#include "unity.h"
#include <stdlib.h>
#include <stdio.h>

void setUp(void) {
    // Setup per ogni test
}

void tearDown(void) {
    // Cleanup dopo ogni test
}

int main(void) {
    UNITY_BEGIN();
    return UNITY_END();
}
